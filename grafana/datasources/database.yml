apiVersion: 1

datasources:
  - name: Database
    type: postgres
    url: database
    database: $POSTGRES_DB
    user: $POSTGRES_USER
    isDefault: true
    secureJsonData:
      password: $POSTGRES_PASSWORD
    jsonData:
      sslmode: $POSTGRES_SSLMODE
